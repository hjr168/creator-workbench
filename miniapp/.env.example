# 小程序构建期注入的环境变量（Taro 读取 TARO_APP_ 前缀）。
# 复制为 .env 后按实际填写。线上必须显式配置 TARO_APP_API_BASE。

# 后端 API 地址。本地开发默认 http://127.0.0.1:3000；
# 线上应改为公网可达的地址（如 https://workbench.huangjiarong.top）。
TARO_APP_API_BASE=http://127.0.0.1:3000

# 小程序访问令牌，需与后端 MINIAPP_API_TOKEN 一致。
# 留空则不带 x-miniapp-token header（仅在后端也未配置 MINIAPP_API_TOKEN 时可用）。
TARO_APP_MINIAPP_TOKEN=
